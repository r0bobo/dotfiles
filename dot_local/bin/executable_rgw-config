#!/bin/bash -eu

cat <<EOF
export AWS_ACCESS_KEY="$(vault kv get -field=access_key "secret/ceph/rgw-users/$1")"
export AWS_SECRET_KEY="$(vault kv get -field=secret_key "secret/ceph/rgw-users/$1")"
EOF
